export default function addNewProject(title, color){
    return new PromiseRejectionEvent()
} 
class Project{
    constructor(title,color){
        this.title = title;
        this.color = color;
    }
}